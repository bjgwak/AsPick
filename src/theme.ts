import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#81c784",
    },
    secondary: {
      main: "#388e3c",
    },
    background: {
      default: "#f5f5f5",
    },
  },
});

export default theme;
